// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // ✅ Any other Next.js config (but NOT middleware matcher)
};

export default nextConfig;